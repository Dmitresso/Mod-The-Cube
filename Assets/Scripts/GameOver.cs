using System;
using System.Runtime.CompilerServices;
using UnityEngine;
using UnityEngine.UI;

public class GameOver : MonoBehaviour {
/*
    private SpriteRenderer spriteRenderer;
    private Image blackImage;
    private Sprite blackSprite;
    private string spriteName = "black";
    private string gameOverText = "Game Over! Your cube scaled to zero" +
                                  " and disappeared into the universe...";
    
    private void Awake() {
        blackImage = GetComponentInChildren<Canvas>().GetComponentInChildren<Image>();
        blackSprite = Resources.Load<Sprite>($"Sprites/{spriteName}");
        blackImage.sprite = blackSprite;
    }
    
    private void Start() {
        var width = blackSprite.bounds.size.x;
        var height = blackSprite.bounds.size.y;        
        blackImage.transform.localScale = new Vector2(Screen.width / width, Screen.height / height);
    }
*/
}